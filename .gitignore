# --- Maven / Java ---
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties

*.class
*.log
hs_err_pid*
replay_pid*

# --- Dự án Chat: file nhận P2P (FileInboundHandler: downloads_<port>/) ---
downloads_*/

# --- Tài liệu cục bộ (báo cáo, nháp — không đẩy lên remote) ---
docs/

# --- Gói / build cục bộ (giữ ngoại lệ wrapper nếu sau này thêm mvnw) ---
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar
!.mvn/wrapper/maven-wrapper.jar

# --- IntelliJ IDEA ---
.idea/
*.iml
*.iws
*.ipr
out/

# --- Eclipse ---
.apt_generated
.classpath
.factorypath
.project
.settings/
.springBeans
.sts4-cache

# --- NetBeans ---
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# --- Gradle (nếu dùng sau này) ---
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# --- VS Code ---
.vscode/

# --- Hệ điều hành ---
.DS_Store
.DS_Store?
._*
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/

# --- Tài liệu / PDF lớn cục bộ (bỏ comment dòng dưới nếu muốn commit file trong repo) ---
# *.pdf


